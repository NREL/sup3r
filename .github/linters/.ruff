name: Lint Code Base

on:
  push:
    branches-ignore:
      - 'gh-pages'

jobs:
  ruff:
    name: Lint Code Base
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: chartboost/ruff-action@v1
