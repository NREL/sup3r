name: Installation Issue
description: Report issues installing sup3r on your system

body:
  - type: checkboxes
    id: checks
    attributes:
      label: Installation check
      options:
        - label: >
            I have read the [installation guide](https://nrel.github.io/sup3r/misc/installation.html).
          required: true
  - type: input
    id: platform
    attributes:
      label: Platform
      description: >
        Please provide the output of ``import platform; print(platform.platform())``
    validations:
      required: true
  - type: dropdown
    id: method
    attributes:
      label: Installation Method
      description: >
        Please provide how you tried to install sup3r from a clean environment.
      options:
        - pip install (from PyPi)
        - pip install . (from source)
        - pixi install
        - Other
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: sup3r Version
      description: >
        Please provide the version of sup3r you are trying to install.
    validations:
      required: true
  - type: input
    id: python
    attributes:
      label: Python Version
      description: >
        Please provide the installed version of Python.
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: Installation Logs
      description: >
        If possible, please copy and paste the installation logs when attempting to install sup3r.
      value: >
        Replace this line with the installation logs
